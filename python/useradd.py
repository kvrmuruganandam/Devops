import os
import crypt
username=raw_input("Enter username:")
passwd=raw_input("Enter password:")
#use=crypt.crypt(passwd,'123')
os.system("adduser "+username)
os.system("passwd "+username <<EOF
        passwd
        passwd
        
        
        
        
        
        )
